interface:
  display_name: "Popclass"
  short_description: "Repo-specific patterns and workflows for popclass"
  default_prompt: "Use the popclass repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true